{
  "borderRadius": {
    "none": { "$value": "0px",    "$type": "dimension", "$description": "No rounding — sharp corners" },
    "xs":   { "$value": "2px",    "$type": "dimension", "$description": "Extra small rounding" },
    "sm":   { "$value": "4px",    "$type": "dimension", "$description": "Small rounding — inputs, badges" },
    "md":   { "$value": "6px",    "$type": "dimension", "$description": "Default rounding — cards, buttons" },
    "lg":   { "$value": "8px",    "$type": "dimension", "$description": "Large rounding — modals, panels" },
    "xl":   { "$value": "12px",   "$type": "dimension", "$description": "Extra large rounding" },
    "2xl":  { "$value": "16px",   "$type": "dimension", "$description": "2x large — floating elements" },
    "3xl":  { "$value": "24px",   "$type": "dimension", "$description": "3x large — prominent cards" },
    "full": { "$value": "9999px", "$type": "dimension", "$description": "Pill shape — tags, toggles" }
  }
}
